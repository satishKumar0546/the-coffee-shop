import { render, screen } from '@testing-library/react';
import App from './App';

test('Navigations should be shown', () => {});
test('Appropriate page should be as per navigation tab', () => {});
test('Coffee list should be shown as per availability', () => {});
test('Coffee sizes should be shown as per availability', () => {});
test('Dialog should be shown up on selection', () => {});
test('List should be as expected upon selection', () => {});
test('Orders number should be as expected as per user selection per coffee per size', () => {});
test('Dialog should be shown upon size button click on orders screen', () => {});
test('Proper user list should be shown upon opening the above dialog', () => {});
test('List should be as expected in payments tab', () => {});
